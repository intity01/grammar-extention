{
    "name": "grammar-checker-extension",
    "version": "1.0.0",
    "description": "Chrome extension for real-time grammar checking of Thai, English, and Japanese.",
    "scripts": {
        "build": "node build.js",
        "format": "prettier --write src"
    },
    "devDependencies": {
        "prettier": "^3.0.0"
    }
}